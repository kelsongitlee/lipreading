# Core dependencies
hydra-core >= 1.3.2
opencv-python >= 4.5.5.62
scipy >= 1.3.0
scikit-image >= 0.13.0
av >= 10.0.0
six >= 1.16.0

# PyTorch ecosystem (these should be installed separately based on CUDA version)
# torch >= 1.9.0
# torchvision >= 0.10.0
# torchaudio >= 0.9.0

# Face detection and alignment
mediapipe >= 0.10.0
ibug-face-detection >= 0.1.0
ibug-face-alignment >= 0.1.0

# Additional dependencies
numpy >= 1.21.0
Pillow >= 8.0.0
matplotlib >= 3.3.0
tqdm >= 4.60.0

# Audio/Video processing
ffmpeg-python >= 0.2.0

# Optional: For better performance
# tensorboard >= 2.8.0 
